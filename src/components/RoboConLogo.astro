---
import "@/globals.css"
interface Props {
    clickable?: boolean;
    href?:string;
}
let { clickable = true, href="/" } = Astro.props
---
<a href={clickable ? href : ""} class="logo text-[hsl(var(--rclogo-foreground))]" >
    ROBOCON
</a>
<style>
    .logo {
        height:1em;
        font-family: "FugazOne";
        font-size: 2em;
        line-height: 1em;
        padding-left: 0.1em;
        padding-right:0.1em;
        text-decoration: none;
        text-shadow: -0.05em 0.05em black;
        cursor: {clickable ? "auto" : "default"};
    }
</style>